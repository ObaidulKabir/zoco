/**
 * Sprint 0 stub. Sprint 2 fills Acme + Nodi Traders personas (ZOQO-QA-001 §6).
 */
export async function seed(): Promise<void> {
  // eslint-disable-next-line no-console
  console.log('seed: no organizations yet (Sprint 2)');
}

if (require.main === module) {
  void seed();
}
